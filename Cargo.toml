[package]
name = "randomos"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.18", features = ["extension-module"] }
rand = "0.8"

[build-dependencies]
pyo3-build-config = "0.18"
